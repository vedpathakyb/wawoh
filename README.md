Web application with out Hibernate. It uses plain JDBC code to insert data into database from a controller servlet. No datasource used or no hibernate used. 
Database requirement: 
Schema named "demo" 
Table named "Emp" with two columns "Id" & "Name" 
